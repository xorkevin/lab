regions:
  900:
    regionid: 900
    regioncode: internal
    regionname: Internal Region
    nodes:
      - name: 900a
        regionid: 900
        hostname: {{ .Vars.service.headscale.derp.host }}
        derpport: {{ .Vars.service.headscale.derp.port }}
