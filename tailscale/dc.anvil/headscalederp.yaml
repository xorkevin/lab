regions:
  900:
    regionid: 900
    regioncode: internal
    regionname: Internal Region
    nodes:
      - name: 900a
        regionid: 900
        hostname: {{ .Vars.server.headscale.derp.domain }}
        derpport: {{ .Vars.server.headscale.derp.port }}
